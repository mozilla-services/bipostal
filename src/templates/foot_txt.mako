<%
    info = pageargs.get('info', {})
%>---
To temporarily disable or prevent more notifications from ${info.get('origin', 'this sender')}, 
go to:
${info.get('manageurl', 'http://bipostal.diresworb.org/#template')}

